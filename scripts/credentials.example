# GitLab Credentials File Example
# Copy this to ~/.config/gitlab/credentials and update with your actual credentials
# Make sure to set appropriate permissions: chmod 600 ~/.config/gitlab/credentials

GITLAB_USERNAME=your_gitlab_username
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# Token Permissions Required:
# - read_repository: To clone and read repository contents
# - read_user: To access user information (if needed)
#
# To create a Personal Access Token:
# 1. Go to GitLab → User Settings → Access Tokens
# 2. Create a new token with the required scopes
# 3. Copy the token value and paste it above
