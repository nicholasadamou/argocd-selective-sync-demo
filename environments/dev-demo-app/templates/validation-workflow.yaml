apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  name: dev-demo-validation-{{ .Values.image.tag | default "latest" }}
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
spec:
  workflowTemplateRef:
    name: dev-validation-workflow
    namespace: argo
  arguments:
    parameters:
    - name: environment
      value: "dev"
    - name: service-name
      value: "demo-app"
    - name: service-type
      value: "demo"
    - name: health-endpoint
      value: "/"
    - name: wait-time
      value: "10"
  serviceAccountName: argo-workflow
  podGC:
    strategy: OnWorkflowCompletion
