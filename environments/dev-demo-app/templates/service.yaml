apiVersion: v1
kind: Service
metadata:
  name: demo-app-dev-service
  labels:
    app: demo-app-dev
    environment: dev
spec:
  selector:
    app: demo-app-dev
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
